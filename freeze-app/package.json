{
  "dependencies": {
    "lit": "^2.0.0-rc.2"
  },
  "scripts": {
    "build": "rimraf out && tsc && rollup -c rollup.config.js",
    "start:build": "npm run build && web-dev-server --root-dir dist --app-index index.html --open --compatibility none",
    "start": "tsc && concurrently -k -r \"tsc --watch --preserveWatchOutput\" \"wds\"",
    "deploy-to-gh-pages": "npm run build && cd out/dist && git init -b gh-pages && git add . && git commit -m \"npm run deploy-to-gh-pages\" && git push --force git@github.com:afq984/ilmsfreeze gh-pages"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^7.1.3",
    "@web/dev-server": "^0.1.18",
    "@web/rollup-plugin-copy": "^0.3.0",
    "@web/rollup-plugin-html": "^1.8.0",
    "concurrently": "^6.2.0",
    "deepmerge": "^4.2.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.52.7",
    "rollup-plugin-minify-html-literals": "^1.2.6",
    "rollup-plugin-summary": "^1.3.0",
    "rollup-plugin-terser": "^7.0.2"
  }
}
